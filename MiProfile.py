class Profile:
    def __init__(self, i):
        self.id = i["id"]
        self.name = i["name"]
        self.username = i["username"]
        self.host = i["host"]
        self.avatar_url = i["avatarUrl"]
